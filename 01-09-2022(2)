#include<stdio.h>
#include<conio.h>
int cal(int n);
void main()
{
	int n;
	printf("enter number");
	scanf("%d",&n);
	printf("\n%d",cal(n));
}
int cal(int n)
{ 
int i,fact=1;
	for(i=n;i!=0;i--)
	fact=(fact*i);

	return fact;
}
Output:
enter number 7

5040
